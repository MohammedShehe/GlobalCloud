name: globalcloud
description: "Global Cloud - Secure Your Family World"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  # For beautiful loading indicators
  flutter_spinkit: ^5.2.1
  # For custom fonts
  google_fonts: ^6.2.1
  intl: ^0.19.0  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png